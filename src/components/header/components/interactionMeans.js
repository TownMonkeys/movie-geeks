import React, { memo } from 'react';
import { 
  StyledInteractionMeans
} from '../style';

const interactionMeans = () => {
  return (
    <StyledInteractionMeans>
      
    </StyledInteractionMeans>
  );
}

export default memo(interactionMeans);