import React, { memo } from 'react';
import { 
  StyledFeaturesList,
  Feature
} from './style';

const FeaturesList = () => {
  return (
    <StyledFeaturesList>
      {/* <Feature>
        
      </Feature> */}
    </StyledFeaturesList>
  );
}

export default memo(FeaturesList);